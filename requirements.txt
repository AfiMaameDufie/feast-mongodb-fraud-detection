# Pinned so the tutorial keeps working as these projects move.
django-mongodb-backend==6.1.0
python-dotenv==1.1.1
feast==0.66.0
pymongo==4.18.1
pandas==2.3.3
numpy==2.5.3
pyarrow==25.0.1
# Not used by the tutorial itself; it is there for the "next steps" exercise
# that swaps the z-score model for a trained classifier.
scikit-learn==1.9.1
